---
// Extends Starlight's default Head to add Astro's ClientRouter, giving the
// docs SPA-like view transitions (no full reload between pages).
import Default from '@astrojs/starlight/components/Head.astro'
import { ClientRouter } from 'astro:transitions'
---

<Default><slot /></Default>
<ClientRouter />
